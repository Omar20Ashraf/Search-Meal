const state = {
    meals: ['Salmon'],
};

const getters = {
    meals: (state) => state.meals,
};

const actions = {

};

const mutations = {

};

export default {
    state,
    getters,
    actions,
    mutations,
};
